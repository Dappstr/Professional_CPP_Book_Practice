int main() {
    //std::vector<bool> v{}; The bool specialization for std::vector is more like a bitfield. Prefer std::bitset if using
    // a set of bits which will not change in size, or use a vector of type `int_fast8_t` or unsigned char

    return 0;
}
